pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                 sh 'docker build -t angular-test .'
            }
        }
         stage('Docker Run') {
            steps {
               sh 'docker run -d -p 8081:80 angular-test'
            }
        }
    }
}
