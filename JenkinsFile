pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                 sh 'docker build -t angular-test .'
            }
        }
         stage('Docker Run') {
            steps {
               sh 'docker run --name angular-test -d -p 8080:80 angular-test'
            }
        }
    }
}
