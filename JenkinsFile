pipeline {
    agent any
    stages {
        stage('Install Packages') {
            steps {
                sh 'npm install'
            }
        }
        stage('Build') {
            steps {
                sh 'sudo npm run build --prod'
            }
        }              
    }
}
