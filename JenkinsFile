pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                 echo 'Hello'
            }
        }
         stage('Docker Run') {
            steps {
                echo 'Hello'
            }
        }
    }
}
